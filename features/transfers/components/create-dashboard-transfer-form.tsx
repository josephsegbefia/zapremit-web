interface CreateDashboardTransferFormProps {
  onCancel?: () => void;
}

export const CreateDashboardTransferForm = ({
  onCancel,
}: CreateDashboardTransferFormProps) => {
  return (
    <div>
      <h1>Transfer form on dashboard page</h1>
    </div>
  );
};
